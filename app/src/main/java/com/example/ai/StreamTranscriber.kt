package com.example.ai

class StreamTranscriber {
    fun processVoiceChunk(pcmBuffer: ByteArray): String {
        // On-device whisper whisper fast inference for closed captions
        return "Transcribed text"
    }
}
